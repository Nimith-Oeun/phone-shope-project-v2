#=======================================================================
#this file is the main file that will be used to run the changelog files
#=======================================================================

spring.application.name=phone_shope_Demo
server.port=8081

#database configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/phone_shope
spring.datasource.username=shop
spring.datasource.password=P@ssw0rd()
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

#config for swagger
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true

#config for Liquibase
spring.liquibase.change-log=classpath:db/changelog/db.changelog-master.yaml
spring.liquibase.enabled=true
spring.liquibase.clear-checksums=false
#spring.liquibase.url=jdbc:postgresql://localhost:5432/phone_shope  # Same as datasource URL
#spring.liquibase.user=shop  # Same as datasource username
#spring.liquibase.password=P@ssw0rd()  # Same as datasource password
#spring.liquibase.default-schema=public